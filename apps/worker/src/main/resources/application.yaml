spring:
  application:
    name: gits-kno-worker
  main:
    banner-mode: "off"

server:
  port: 8090

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true

gits:
  worker:
    enabled: true
