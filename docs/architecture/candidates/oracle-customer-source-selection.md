# Customer Source Selection — status after ADR-0010

> **FORMALIZED by ADR-0010 / loop P4.** This note is retained as discovery history.
> Authoritative artifacts:
> - Semantic class: `gits:Customer` in `specs/semantic/gits-core.{owl.ttl,linkml.yaml}`
> - Source Contract: `specs/data/src-edwcrm-cust-base.v0.1.json` (`CTR-DATA-002`)
> - Mapping: `specs/data/customer-source-mapping.r2rml.ttl` (`CTR-MAP-001`, `versioned_mapping`)
>
> Customer remains **outside** the relational operational control plane.
