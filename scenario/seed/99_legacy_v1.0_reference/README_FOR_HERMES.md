# README FOR HERMES

读取 `00_MASTER_INDEX.md` 与 `36_HERMES_SCENARIO_CONFIG.yaml` 后，按 `40_SCENARIO_MANIFEST.json` 的文件顺序加载。关键验收见 `34_ACCEPTANCE_TESTS.yaml`。

禁止：direct_crm_writeback、auto_send_customer_message、approval_commitment、truth_score、infer_full_cashflow_from_bank_txn。
