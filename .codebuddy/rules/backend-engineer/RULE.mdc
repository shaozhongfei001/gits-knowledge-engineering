---
description: "后端工程师规范：Port/Adapter 模式、领域建模、测试策略、迁移规范、配置管理"
alwaysApply: false
globs: "modules/**,adapters/**,apps/**"
enabled: true
---

# 后端工程师规范

## 1. Port / Adapter 分层

### modules/（领域模块）

- **纯业务逻辑**，零框架依赖（不依赖 Spring、MyBatis、Jena 等）。
- 通过 `port/` 子包定义接口（Port），供外部实现。
- 领域对象使用 Java record 或不可变类。
- Service 注入 Port 接口，不注入具体实现。

```
modules/operational-ontology/
├── domain/          # 领域对象（Customer, Claim, Interaction...）
├── port/            # Port 接口（SemanticRepositoryPort, ClaimReconciliationPort...）
└── service/         # 领域服务（KycInsightService, ClaimService...）
```

### adapters/（防腐层适配器）

- 实现 modules/ 中定义的 Port 接口。
- 负责与外部系统（数据库、语义引擎、Oracle 等）对接。
- 数据转换：外部格式 ↔ 领域对象。

```
adapters/persistence-relational/   # 实现 Repository Port
adapters/semantic-jena/            # 实现 SemanticRepositoryPort
adapters/oracle-source/            # 实现 OracleSourcePort（隔离）
```

### apps/（应用组装）

- Spring Boot 启动类 + 配置。
- 通过 `@Configuration` 选择 Port 的具体实现。
- Controller / DTO / Security 配置。

## 2. 领域建模规范

- 值对象：使用 Java record（如 `ClaimId`, `CustomerId`, `EvidenceBundle`）。
- 实体：使用不可变类 + `withXxx()` 方法返回新实例。
- 领域事件：使用 CloudEvent record，事件类型复用 `DomainEventType` 常量。
- 异常：领域异常继承 `RuntimeException`，携带领域语义（如 `ClaimConflictException`）。

## 3. 新增 Port 接口检查清单

1. 在 `modules/<module>/port/` 下定义接口。
2. 在 `specs/CONTRACT_INDEX.yaml` 中注册（如涉及合同变更）。
3. 在 `adapters/` 下提供至少一个实现。
4. 在 `apps/` 的 `@Configuration` 中装配。
5. 编写 Port 接口的单元测试（Mock 实现）。
6. 编写 Adapter 的集成测试（真实外部系统或 Testcontainers）。

## 4. 测试策略

| 层 | 测试类型 | 工具 | 命名 |
|----|----------|------|------|
| modules/ | 单元测试 | JUnit 5 + AssertJ + Mockito | `*Test.java` |
| adapters/ | 集成测试 | `@SpringBootTest` + H2/Testcontainers | `*IT.java` |
| apps/ | E2E/合同测试 | MockMvc + springdoc | `*IT.java` |

- **先写测试**：TDD 红-绿-重构。
- Service 测试：Mock Port 接口，验证业务逻辑。
- Adapter 测试：使用 H2 内存库或 Testcontainers，验证 SQL 和映射。
- Controller 测试：MockMvc 验证 HTTP 契约。

## 5. 数据库迁移规范

- 迁移脚本放在 `adapters/persistence-relational/src/main/resources/db/migration/`。
- H2 方言：`h2/` 子目录；MySQL 方言：`mysql/` 子目录。
- 命名：`V{NNN}__{snake_case_description}.sql`（双下划线）。
- 每个迁移只做一件事（单一职责）。
- 禁止修改已执行的迁移脚本（追加新迁移修复）。
- 新增表必须包含 `created_at`、`updated_at` 列。

## 6. 配置管理

- 应用配置：`apps/api/src/main/resources/application.yaml`。
- Profile 配置：`application-{profile}.yaml`（local, mysql, oracle, staging, prod）。
- 自定义配置类：`EngagementConfig`、`RepositoryConfig` 等，使用 `@ConfigurationProperties`。
- 禁止在代码中硬编码配置值（URL、密码、超时等）。
- 敏感配置使用环境变量覆盖。

## 7. 日志规范

- 使用 SLF4J + Logback。
- 结构化日志：JSON 格式（生产环境）。
- 日志级别：
  - ERROR：系统错误，需要人工介入。
  - WARN：业务异常，可自动恢复。
  - INFO：关键业务操作（创建客户、提交 Claim 等）。
  - DEBUG：调试信息（仅开发环境）。
- 禁止在日志中输出 API Key、密码等敏感信息。

## 8. 并发与幂等

- 写操作必须幂等：相同请求多次执行结果一致。
- 使用乐观锁（版本号）或唯一约束防止并发冲突。
- 领域事件发布必须与业务操作在同一事务内（或使用 Outbox 模式）。

## 9. Spring Modulith 约束

- 模块间依赖必须显式声明（`@DependsOn`）。
- 禁止循环依赖。
- 内部类/方法使用 `package-private` 或 `internal` 标记，不暴露给其他模块。
- 跨模块调用必须通过 Port 接口，禁止直接注入其他模块的 Service。
