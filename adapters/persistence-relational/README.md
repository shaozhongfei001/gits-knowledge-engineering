# Relational persistence adapter

The SQL migration is a MySQL 8.4 local-validation candidate for authoritative operational objects. It does not decide the production database. Production use requires ADR-0003, bank compatibility, HA, backup, encryption, retention, capacity and migration review.
