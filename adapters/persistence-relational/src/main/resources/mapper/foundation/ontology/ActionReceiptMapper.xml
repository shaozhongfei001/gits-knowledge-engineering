<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gien.gits.adapter.persistence.foundation.ontology.mapper.ActionReceiptMapper">

    <resultMap id="actionReceiptResult" type="com.gien.gits.ontology.ActionReceipt">
            <constructor>
                <idArg column="action_id" javaType="java.util.UUID"/>
                <arg column="target_system" javaType="String"/>
                <arg column="external_id" javaType="String"/>
                <arg column="result_status" javaType="String"/>
                <arg column="result_message" javaType="String"/>
                <arg column="completed_at" javaType="java.time.Instant"
                    typeHandler="com.gien.gits.adapter.persistence.common.typehandler.InstantTypeHandler"/>
            </constructor>
</resultMap>

    <insert id="insert" parameterType="com.gien.gits.ontology.ActionReceipt">
        INSERT INTO action_receipt (
            action_id, target_system, external_id, result_status, result_message, completed_at
        ) VALUES (
            #{actionId}, #{targetSystem}, #{externalId}, #{resultStatus}, #{resultMessage},
            #{completedAt, typeHandler=com.gien.gits.adapter.persistence.common.typehandler.InstantTypeHandler}
        )
    </insert>

    <select id="findByActionId" resultMap="actionReceiptResult">
        SELECT action_id, target_system, external_id, result_status, result_message, completed_at
        FROM action_receipt WHERE action_id = #{actionId}
    </select>

</mapper>
