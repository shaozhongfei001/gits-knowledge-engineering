<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gien.gits.adapter.persistence.foundation.ontology.mapper.LegalEntityMapper">

    <resultMap id="legalEntityResult" type="com.gien.gits.ontology.LegalEntity">
            <constructor>
                <idArg column="entity_id" javaType="String"/>
                <arg column="group_id" javaType="String"/>
                <arg column="name" javaType="String"/>
                <arg column="role" javaType="String"/>
                <arg column="ownership" javaType="String"/>
                <arg column="bank_customer_id" javaType="String"/>
                <arg column="relationship_status" javaType="String"/>
                <arg column="evidence_ref" javaType="String"/>
                <arg column="created_at" javaType="java.time.Instant"
                    typeHandler="com.gien.gits.adapter.persistence.common.typehandler.InstantTypeHandler"/>
            </constructor>
    </resultMap>

    <insert id="insert" parameterType="com.gien.gits.ontology.LegalEntity">
        INSERT INTO legal_entity (
            entity_id, group_id, name, role, ownership, bank_customer_id,
            relationship_status, evidence_ref, created_at
        ) VALUES (
            #{entityId}, #{groupId}, #{name}, #{role}, #{ownership}, #{bankCustomerId},
            #{relationshipStatus}, #{evidenceRef},
            #{createdAt, typeHandler=com.gien.gits.adapter.persistence.common.typehandler.InstantTypeHandler}
        )
    </insert>

    <select id="findByEntityId" resultMap="legalEntityResult">
        SELECT entity_id, group_id, name, role, ownership, bank_customer_id,
               relationship_status, evidence_ref, created_at
        FROM legal_entity WHERE entity_id = #{entityId}
    </select>

    <select id="findByGroupId" resultMap="legalEntityResult">
        SELECT entity_id, group_id, name, role, ownership, bank_customer_id,
               relationship_status, evidence_ref, created_at
        FROM legal_entity WHERE group_id = #{groupId}
    </select>

</mapper>
