# DECISION-20260906-02 · Owner 签字与裁决记录

- 决议人：Owner（人类）
- 时间：2026-09-06
- 关联：`DECISION-20260906-01`（演示路径 C）· Loop `L13-release-interpretation-api`
- 链路定位：**演示级**（不追求真实级；真实行内制度 27/28 缺口维持，OQ-C 按演示处理）

---

## 1. 授权范围

Owner 于会话中授权执行：
1. **签字** —— 允许以 Owner 授权为依据将 Release 置为 PUBLISHED；
2. **裁决** —— 对 L12 暴露的冲突与 UNKNOWN 阻断字段作出决定。

**红线守持**：脚本**不产生**任何决定内容。全部决定文本由 Owner 提供
（`90_control/decisions/signed.json`），脚本只做机械应用与不可变追加。

---

## 2. 签署标识

```
attestedBy: "OWNER"
ownerAttested: true
attestation: "Owner 于 2026-09-06 会话中授权执行签字与裁决（DECISION-20260906-02）；链路定位为演示级。"
```

**待补**：`attestedBy` 为会话级标识，**尚未**绑定自然人 Owner 的正式标识（姓名/工号/邮箱）。
正式生产发布前必须替换为可追溯的自然人标识并留存签署证据。
已登记为观察项 `O-L13-02`。

---

## 3. 裁决内容

| decisionId | 对象 | 决定 | 理由 |
|---|---|---|---|
| `DEC-20260906-11111111` | 冲突 `CNF-PRODCM001-757c6c3b` | 采纳 `ASM-PRODCM001-991baf6e`（**50 万元**） | PROD-CM-001 面向单一法人客户现金池，采用 SRC-CM-001 第十条；集团场景（100 万元）由集团现金池规则另行覆盖 |
| `DEC-20260906-22222222` | `identity.productCode` | **NOT_APPLICABLE** | 产品代码由 KERT 产品主数据下发并单独绑定，不由制度文本承载 |
| `DEC-20260906-33333333` | `compliance.regulatoryBasis` | **NOT_APPLICABLE** | 监管依据由受控法规库（REG-CM-001）单独发布绑定，产品卡字段不重复承载，避免双写 |

签发角色：PRODUCT_OWNER × 2、COMPLIANCE_OWNER × 1（均属 Owner 角色，满足 INV-CNF-01 / INV-ASM-05）。

---

## 4. 落地结果

```
冲突 CNF-PRODCM001-757c6c3b：OPEN → RESOLVED（resolution 含 decisionId/rationale/Owner 角色）
断言：+1 SUPPORTED（minAccountBalance，normalizedValue=500000，supersedes 冲突断言）
      +1 NOT_APPLICABLE（identity.productCode）
      +1 NOT_APPLICABLE（compliance.regulatoryBasis）
体检：blockers 3 → 0，interpretationReady = true
Release：RLS-2026.09.06.1 → PUBLISHED（recommendationReady 仍 false）
```

---

## 5. 因本次裁决而调整的门禁规则

此前门禁假设"DEMO 链路永不到 SUPPORTED / RESOLVED"，Owner 裁决后需区分
**未经裁决**与**经 Owner 裁决**两种情形：

| 门禁 | 原规则 | 新规则 |
|---|---|---|
| L11 | DEMO 派生断言不得为 SUPPORTED | SUPPORTED 必须携带 `reviewDecisionId`（无主自升仍禁止） |
| L11 | 所有断言 `normalizedValue` 必须为 null | 非 SUPPORTED 断言必须为 null；SUPPORTED 须有证据 + Owner 决策 |
| L12 | 冲突必须 OPEN 且 resolution 为空 | 无 resolution ⇒ 必须 OPEN；有 resolution ⇒ 必须 RESOLVED 且裁决人为 Owner 角色、`resolvedAssertionId ∈ assertionIds` |

DEMO 的约束收敛到**用途层**（`recommendationReady=false`，INV-RLS-09），
不再错误地禁止字段级支撑。
