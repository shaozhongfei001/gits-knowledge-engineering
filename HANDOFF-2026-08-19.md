# 交接文件｜2026-08-19

> 主工作区：`/home/szf/dev/gits-knowledge-engineering`（唯一 workspace）
> 分支：`main`（HEAD `fd12d6f`，与 `origin/main` 同步）
> 状态：git 工作区完全干净

---

## 1. 当前交付全貌（已合并到 main）

长程任务三个核心 Loop 已全部闭环并 `qa_pass`：

| Loop | 主题 | 状态 | 提交 |
|---|---|---|---|
| **P19** | V1.1 演示就绪修复 + 工程防护网（enum-consistency / secret-scan / e2e-29） | `qa_pass` | 并入 main |
| **P20** | Wiki-first × Ontology 融合 shadow（知识地图 + ActivationPlan + 受控语义查询 + 上下文装配） | `qa_pass`，6/6 gate | 并入 main |
| **P21** | 生产就绪**准备**（prod fail-closed + cutover/回滚计划 + 真实平台 Port 合同候选） | `qa_pass`，6/6 gate | `d314589` |

### P21 交付物（最新，最近提交）
- `apps/api/src/main/resources/application-prod.yaml`：`gits.security.api-key` 改为 fail-closed（`${API_KEY}` 无默认）
- `apps/api/.../config/ProdConfigValidator.java`：prod 启动时校验 api-key / llm(real).api-key / crm(http).writeback-url 非空（fail-closed）
- `scripts/verify_prod_profile_fail_closed.sh`：生产 profile fail-closed 验证
- `docs/production/P21-CUTOVER-PLAN.md`：分阶段 cutover C0-C4 + 回滚方案（preparation only）
- `docs/production/P21-REAL-PLATFORM-PORTS.md`：真实平台接入 Port 合同候选（RagEmbeddingPort / KnowledgeGraphPort / MetadataCatalogPort）
- `scripts/verify_production_plan.sh`：生产计划验证

### 验证口径
- 后端：apps/api 317 tests + worker 22 tests，0 fail
- 前端：vue-tsc + vitest 100 tests + vite build
- dependency-check：15 reports 全 PASS
- **环境约束**：`make verify` 的 `db-check` 需外部 `GITS_KEDB_PASSWORD`（P19/P21 用 H2 不需 MySQL）；RetireJS 数据源网络不可达，`make backend-test` 需设 `MAVEN_OPTS="-Ddependency.check.auto.update=false"` 离线跑

---

## 2. 后续工作项（按优先级）

### A. P20 真实平台接入（生产就绪推进）
- **P21 已产出**真实平台 Port 合同候选：`RagEmbeddingPort` / `KnowledgeGraphPort` / `MetadataCatalogPort`（见 `docs/production/P21-REAL-PLATFORM-PORTS.md`）
- **下一步（需 Owner 授权）**：登记到 `specs/CONTRACT_INDEX.yaml` → `make generate` → `make check`，再实现适配器
- **关键约束**：这些候选合同**尚未登记**，禁止直接实现

### B. 生产 cutover / fusion（需 Owner 单独批准）
- P20/P21 均为 **preparation only**：`FUSION_CUTOVER=NOT_EXECUTED`、`PRODUCTION_CUTOVER=NOT_EXECUTED`
- 实际生产切换、fusion 切换、生产写回**均需 Owner 另行批准**后执行（见 `docs/production/P21-CUTOVER-PLAN.md`）

### C. OpenWiki（已决策：暂缓）
- Owner 已采纳"暂缓（DEFER_UNTIL_PRODUCTION_READY）"（`docs/architecture/OPENWIKI_PROJECTION_ASSESSMENT.md`）
- ADR-0017 保持 `PROPOSED_FOR_P20_OWNER_REVIEW`，待 P20 转生产就绪或出现明确业务需求再评估

### D. P7 正式独立 QA（待办）
- P7（MySQL 兼容性）为历史收尾 `closed`，**正式独立 QA 未执行**（如实标注）
- 需**单独工作项**，在有 MySQL 凭据环境执行（T4 MySQL 启动验证 + T5 独立 QA）

### E. 其他遗留
- `specs/CONTRACT_INDEX.yaml` 无 OpenWiki 记录（尚未登记为受控资产）
- Oracle 写回属 quarantine 隔离资产，启用需 ADR + 数据所有者授权 + 专用 Loop

---

## 3. 关键约束与红线

1. **合同 SSOT**：`specs/CONTRACT_INDEX.yaml` 是唯一合同注册入口；改合同必须先改源 → `make generate` → `make check` → 再写实现
2. **禁止改 `generated/`**：只读，由 `make generate` 生成
3. **开发不得自签 QA_PASS**：只有独立 QA 角色（actor ≠ feature_pilot/tech_lead）才能记录
4. **失败先记录** `FAILURES.md`，再修复
5. **禁止 `git add .`**：显式添加文件
6. **禁止跨 Loop 越界**：只在当前 Loop scope 内工作
7. **隔离资产**：Oracle 等未授权资产在 `tools/quarantine/`，无授权不得引用
8. **生产冻结**：P20/P21 未获 Owner 批准前不得执行生产切换/写回

---

## 4. 环境备忘

- **系统 Python 3.10 < 3.11**（bootstrap-check 要求 3.11+）：已装 Python 3.14，路径 `/home/szf/.workbuddy/binaries/python/versions/3.14.3/bin`；跑 `make verify` 时前置 PATH
- **dependency-check 离线**：RetireJS 数据源不可达，需 `MAVEN_OPTS="-Ddependency.check.auto.update=false"`；损坏缓存文件删除即可
- **前端 npm ci**：npm 11 需 `npm_config_legacy_peer_deps=true`（或 `npm ci --legacy-peer-deps`）
- **db-check**：需 `GITS_KEDB_PASSWORD`（外部 MySQL 凭据）

---

## 5. 收尾确认

- [x] 三个 Loop（P19/P20/P21）全部 `qa_pass` 合并到 main
- [x] 单一 workspace 收敛完成
- [x] git 工作区干净，`main` = `origin/main` = `fd12d6f`
- [x] 清理 `-p20` worktree、`feature/p19` 分支、`generated-images/`

---

*本交接文件由 CodeBuddy 于 2026-08-19 生成，供后续会话/开发者参考。*
