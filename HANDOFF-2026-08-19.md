# 交接文件｜2026-08-19

> 主工作区：`/home/szf/dev/gits-knowledge-engineering`（唯一 workspace）
> 分支：`main`（HEAD `49b175f`，与 `origin/main` 同步）
> 状态：git 工作区完全干净

---

## 1. 当前交付全貌（已合并到 main）

长程任务四个核心 Loop 已全部闭环并 `qa_pass`：

| Loop | 主题 | 状态 | 提交 |
|---|---|---|---|
| **P19** | V1.1 演示就绪修复 + 工程防护网（enum-consistency / secret-scan / e2e-29） | `qa_pass` | 并入 main |
| **P20** | Wiki-first × Ontology 融合 shadow（知识地图 + ActivationPlan + 受控语义查询 + 上下文装配） | `qa_pass`，6/6 gate | 并入 main |
| **P21** | 生产就绪**准备**（prod fail-closed + cutover/回滚计划 + 真实平台 Port 合同候选） | `qa_pass`，6/6 gate | 并入 main |
| **P22** | **LLM-WIKI 知识地图/目录**（对齐权威规范，增强 Git 控制面，LLM 读图方案 A） | `qa_pass`，9/9 gate | `49b175f` |

### P22 交付物（最新）
- **合同**：`KnowledgeElement`（`CTR-KELEM-001`）登记到 `specs/CONTRACT_INDEX.yaml`；`knowledge-element.schema.json`（K-Type-F/R/P/E/M）
- **知识资产**：39 个 KE 资产化到 `specs/knowledge-architecture/elements/{KI}/KE-*.md`（对齐《银行知识工程规范打样_fixed.xlsx》）
- **控制面（E1）**：`InMemoryKnowledgeStore` + `KnowledgeSnapshotLoader` + 5 个 `InMemory*Reader`，装配进 `apps/api`
- **LLM 读图（G3）**：`KnowledgeWikiPort` + `KnowledgeWikiFilesystemAdapter` + `KnowledgeWikiService`（方案 A：规划器决定加载范围）
- **Shadow E2E（G4）**：`run_p22_shadow_e2e.py`（两场景黄金比对 + LLM 读图导航，formal_output_changed=false）
- **设计/决策**：`docs/design/LLM-WIKI-KNOWLEDGE-MAP.md`、`docs/design/P22-GIT-CONTROL-PLANE-PRODUCTION.md`、`docs/architecture/OPENWIKI_CODE_LEVEL_ASSESSMENT.md`、`docs/design/knowledge-engineering-map-ref.json`
- **架构图**：`diagrams/`（architecture + flow 的 HTML/PNG/MD + 生成脚本）

### 验证口径
- 后端：apps/api 321 tests + worker 22 tests，0 fail（覆盖率达标）
- 前端：vue-tsc + vitest + vite build 通过
- dependency-check：15 reports 全 PASS
- 两场景 shadow E2E 黄金比对一致 + LLM 读图导航
- **环境约束**：`make verify` 的 `db-check` 需外部 `GITS_KEDB_PASSWORD`（P22 shadow 不写 DB，未纳入）；RetireJS 数据源网络不可达，需 `MAVEN_OPTS="-Ddependency.check.auto.update=false"` 离线跑

---

## 2. 后续工作项（按优先级）

### A. 真实平台接入（需 Owner 授权，选型留空）
- P21 已产出真实平台 Port 合同候选：`RagEmbeddingPort` / `KnowledgeGraphPort` / `MetadataCatalogPort`（`docs/production/P21-REAL-PLATFORM-PORTS.md`）
- **P22 已登记** `KnowledgeElement`（`CTR-KELEM-001`），但**真实平台技术选型（RAG 引擎 / GraphDB / MetadataCatalog）仍留空待 Owner 指定**——禁止臆测选型实现
- **下一步（需 Owner 授权 + 定选型）**：登记到 `specs/CONTRACT_INDEX.yaml` → `make generate` → `make check` → 实现适配器

### B. 生产 cutover / fusion（需 Owner 单独批准）
- P20/P21/P22 均为 **preparation only**：`FUSION_CUTOVER=NOT_EXECUTED`、`PRODUCTION_CUTOVER=NOT_EXECUTED`（P22 shadow 不写 DB）
- 实际生产切换、fusion 切换、生产写回**均需 Owner 另行批准**后执行（见 `docs/production/P21-CUTOVER-PLAN.md`）

### C. OpenWiki（已决策：放弃引入，但保留人机共读目标）
- **P22 代码级评估**（`docs/architecture/OPENWIKI_CODE_LEVEL_ASSESSMENT.md`）：`langchain-ai/openwiki` 是 **Agent 文档生成 CLI**，非权威知识平台；权限缺失 / 内网 CDN / 非结构化三大硬伤，**决定不引入**
- **保留"人机共读知识地图/目录/契约"目标**：增强 Git/文件系统控制面至生产可用（P22 E1/G3 已实现），人机共读的"人侧"投影层（GAP-5）过渡期用只读导航，后续评估
- ADR-0017 边界（权威源永远是 `specs/`）保持为强制约束

### D. P7 正式独立 QA（待办）
- P7（MySQL 兼容性）为历史收尾 `closed`，**正式独立 QA 未执行**（如实标注）
- 需**单独工作项**，在有 MySQL 凭据环境执行（T4 MySQL 启动验证 + T5 独立 QA）

### E. 其他遗留
- `specs/CONTRACT_INDEX.yaml` 无 OpenWiki 记录（尚未登记为受控资产）
- Oracle 写回属 quarantine 隔离资产，启用需 ADR + 数据所有者授权 + 专用 Loop

---

## 3. 关键约束与红线

1. **合同 SSOT**：`specs/CONTRACT_INDEX.yaml` 是唯一合同注册入口；改合同必须先改源 → `make generate` → `make check` → 再写实现
2. **禁止改 `generated/`**：只读，由 `make generate` 生成
3. **开发不得自签 QA_PASS**：只有独立 QA 角色（actor ≠ feature_pilot/tech_lead）才能记录
4. **失败先记录** `FAILURES.md`，再修复
5. **禁止 `git add .`**：显式添加文件
6. **禁止跨 Loop 越界**：只在当前 Loop scope 内工作
7. **隔离资产**：Oracle 等未授权资产在 `tools/quarantine/`，无授权不得引用
8. **生产冻结**：P20/P21 未获 Owner 批准前不得执行生产切换/写回

---

## 4. 环境备忘

- **系统 Python 3.10 < 3.11**（bootstrap-check 要求 3.11+）：已装 Python 3.14，路径 `/home/szf/.workbuddy/binaries/python/versions/3.14.3/bin`；跑 `make verify` 时前置 PATH
- **dependency-check 离线**：RetireJS 数据源不可达，需 `MAVEN_OPTS="-Ddependency.check.auto.update=false"`；损坏缓存文件删除即可
- **前端 npm ci**：npm 11 需 `npm_config_legacy_peer_deps=true`（或 `npm ci --legacy-peer-deps`）
- **db-check**：需 `GITS_KEDB_PASSWORD`（外部 MySQL 凭据）

---

## 5. 收尾确认

- [x] 四个 Loop（P19/P20/P21/P22）全部 `qa_pass` 合并到 main
- [x] 单一 workspace 收敛完成
- [x] git 工作区干净，`main` = `origin/main` = `49b175f`
- [x] 清理 `-p20` worktree、`feature/p19` 分支、`generated-images/`
- [x] P22 放弃 OpenWiki（代码级评估），增强 Git 控制面，保留人机共读目标
- [x] P22 已推送 origin/main，含 `diagrams/` 架构图资产

---

*本交接文件由 CodeBuddy 于 2026-08-19 生成，供后续会话/开发者参考。*
